# Minimal backend — no TensorFlow (avoids large install and Scripts folder issues)
# Use this inside a virtual environment: python -m venv .venv then .venv\Scripts\activate

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0
pydantic-settings>=2.1.0
pydantic[email]>=2.5.0
python-dotenv>=1.0.0

# Required by backend controllers
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
openpyxl>=3.1.0

# Auth (JWT + password hashing)
# Pin bcrypt<4.1 for passlib compatibility (passlib reads bcrypt.__about__ removed in 4.1+)
bcrypt>=4.0.0,<4.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
